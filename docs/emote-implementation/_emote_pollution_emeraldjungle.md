# emeraldjungle — emote review

## Skipped as suspected ref pollution
- none

## Duplicate/orphan quest files noticed
- `Spirit_Sentinel.pl` + `Spirit_Sentinel_.pl` — two files with near-identical name (trailing underscore); look intentional (two separate Spirit Sentinel NPCs handling different Shaman Epic 1.0 dialogue branches), left alone.
- `6289.pl` + `Scout_Vyrak.pl` — both attach to NPCID 6289 (Scout Vyrak). Name-based takes precedence; `6289.pl` is a stripped Hail-only duplicate that should probably be removed. Not touched (no emote data involved).
